export interface PostMetaData {
    title: string;
    slug: string;
    body: string;
    date: string;
    preview: string;
    category: string;
    author: { name: string, thumbnailUrl: string }
}